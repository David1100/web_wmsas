/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['dglb26w8rx2ld.cloudfront.net','www.sabatebarcelona.com','resoluciongrafica.cl'],
      },
};

export default nextConfig;
